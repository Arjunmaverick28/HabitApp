# Used only if you run the standalone `sonar-scanner` CLI instead of `mvn sonar:sonar`.
# The pom.xml already carries the same settings for the Maven-based scan.

sonar.projectKey=habit-tracker
sonar.projectName=Habit Tracker
sonar.projectVersion=0.0.1-SNAPSHOT

sonar.sources=src/main/java
sonar.tests=src/test/java
sonar.java.binaries=target/classes
sonar.java.test.binaries=target/test-classes

sonar.sourceEncoding=UTF-8
sonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml

sonar.host.url=http://localhost:9000
